'use client';

// import { useRouter } from 'next/router';

export default function DashboardPage() {
    return (
        <div>
            <h1>Reset pw!</h1>
            {/* <button onClick={() => {
                const router = useRouter();
                router.push('/login');
            }}>
                Log out
            </button> */}
        </div>
    );
}
